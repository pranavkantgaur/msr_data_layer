# MSR Knowledge Base Service – Lambda dependencies
#
# These requirements are for the AWS Lambda deployment.
# They extend requirements_mcp.txt with Lambda-specific packages.
#
# Install locally:    pip install -r requirements_lambda.txt
# Used by SAM build:  sam build  (reads this file via Metadata.BuildMethod)

# Core requirements (same as requirements_mcp.txt)
numpy>=1.24.0

# AWS SDK – S3 sync for persistent KB storage
# (pre-installed in Lambda runtime but listed here for local dev + sam build)
boto3>=1.34.0

# Testing
pytest>=8.0.0
